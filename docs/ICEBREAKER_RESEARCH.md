# Icebreaker (Wall-Eating) — Research

**Статус: ПОД ВОПРОСОМ → идёт месячный faithful-тест.** Предварительно: бэктест эдж не доказывает (после комиссий отрицательный), реальные данные подтверждают возражения.

## Концепция (из описания заказчика)

Ловить «поглощение стены»: плотность ≥ $100k в стакане неликвида исчезает → если в этом ценовом интервале реальные сделки в нужную сторону набрали 100% размера стены, это пробой → вход по тренду. Подтверждение ростом OI (Coinglass ~10%). SL −0.15%, TP +0.5%. Сигнал на любой бирже (MEXC/KuCoin/…), вход **всегда на Bybit**.

## Их бэктест (KuCoin lv50 + тики, 1–31 марта 2026, без OI, big-cap исключены)

- 264 трейда, WR 31%, **+10.75% брутто**.
- **Без 2 лучших монет (TAO +4.78, SIREN +3.94) → +2.03%** — 81% эджа в 2 монетах из 35.
- 467 монет без трейдов, 35 с трейдами.

## Квант-пасс (наш) — предварительный вердикт: эдж не доказан

Решающее: безубыточная стоимость = 10.75% / 264 = **0.041%/сделку**. Bybit taker round-trip = 0.11% — **в 2.7× больше всего эджа на сделку**.

| Стресс | Итог |
|---|---|
| Брутто | +10.75% |
| + тейкер 0.11% | **−18.3%** |
| + тейкер + слиппедж | −31% … −47% |
| Bootstrap по монетам (gross) | CI₅₋₉₅ [+0.5, +22.7], P(≤0)=4% |
| Deflated Sharpe (net) | FAIL (p=1.0) |
| MinBTL | INSUFFICIENT (1 мес vs 1–4.6 года) |

## Дополнительные возражения

1. **OI-фильтр (ядро тезиса) не протестирован** и конфликтует по таймингу: Coinglass обновляет OI минутами, сделка живёт секунды.
2. **Кросс-биржевая премиса не проверена бэктестом** (он single-exchange KuCoin). Стена на MEXC ≠ торгуемое движение на Bybit (базис, латентность).
3. **Эппс-эффект:** на неликвидах суб-секундный кросс-биржевой лаг практически неизмерим.

## $0-проба на реальных бесплатных данных (Bybit ob200 архив, 2026-06-16)

Источник: `quote-saver.bycsi.com/orderbook/linear/<SYM>/<date>_<SYM>_ob200.data.zip` — формат = наш WS-формат, реплеится `BookState`/`parse_bybit` без изменений.

| | BANUSDT (дешёвый, $0.075) | TAOUSDT (их топ, ~$300) |
|---|---|---|
| Max стена за день | $19k | $640k |
| Уровней ≥$100k | 0 | 1335 |
| Стен-событий ≥$100k | — (порог недостижим) | 3963 |
| **отменено / съедено** | 14028:1 (@$10k) | **3958 / 5** |
| Сигналы eaten: TP/SL/open | 0/1/0 | **0 / 3 / 2** |
| Net PnL (taker 0.11%) | −0.26% | **−1.00% / 5 сделок** |

**Выводы пробы:**
1. Пайплайн работает на бесплатных данных за **$0** (наш `BookState`/`parse_bybit` реплеит архив Bybit).
2. **Порог $100k coin-зависим** — недостижим на дешёвых неликвидах (макс стена BAN = $19k). Объясняет концентрацию эджа в дорогих TAO/SIREN.
3. **Исчезновение стены ≈ отмена, не поглощение:** даже на TAO 3958 pulled vs 5 eaten (~800:1). Премиса «стена пропала → её съели» эмпирически неверна.
4. **Даже реальные пробои убыточны** (TAO: 0 TP / 3 SL за день).

**Оговорки пробы:** 1 день; «eaten» = прокси «цена прошла сквозь», а не их трейд-подтверждённая абсорбция; single-exchange; TP/SL по mid 200мс.

## Данные (всё бесплатно)

- **Bybit OB:** `quote-saver.bycsi.com/orderbook/linear/<SYM>/<date>_<SYM>_ob200.data.zip` (ob200, snapshot+delta, с 2023-04-05). Покрывает и неликвиды.
- **Bybit трейды:** `public.bybit.com/trading/<SYM>/<SYM><date>.csv.gz` (aggressor side).
- KuCoin Futures L2 — публичный архив lv50 (их источник) + Tardis-копия с 2024-01-25.
- Tardis free-сэмплы (1-е число месяца) — для кросс-валидации.
- Покупка не требуется. Tardis (~$200–600/мес) — опция за удобство/нормализацию.

## Месячный faithful-тест (идёт)

Воспроизводим **их период март-2026** на Bybit (free), монеты с реальными стенами: TAO, SIREN, ZEC, SUI. Данные качаются и сохраняются в **едином parquet-формате** (`exchange/symbol/date/{book_diff,trades}`, та же схема, что у live-коллектора — переиспользуемо).
Сигнал — **точный по их определению**: стена ≥$N исчезла + трейды на этом уровне в нужную сторону ≥ размера стены (абсорбция). Цель: сотни сигналов → окончательный вердикт по hit-rate и net PnL.

## Инфраструктура (готова, ветка `icebreaker-collector`)

- `src/icebreaker/` — universe, book (L2 реконструкция), recorder (parquet), parse_bybit, collector, live_bybit, watchlist. 73 unit-теста.
- Live-коллектор проверен на серваке (smoke 60с, 0 resync/reconnect, ~17 MB/символ/день).
- Скрипты: `icebreaker_preflight.py`, `run_icebreaker_collector.py`, `icebreaker_wall_scan.py`.
